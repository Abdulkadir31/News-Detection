# News-Detection
An implementation of Passive Aggressive Classifier for detecting real or fake news.

1. In order to replicate the results, install and extract the project into a folder
2. Run **pip install requirements.txt** to install the dependencies
3. Run news_detector.py file to run the code 
